/**
 * Index for Home components used exclusively inside the
 * Home pages (apart from FAQ, CTA and SocialProof that are also
 * used inside the main services page)
 */

import Benefits from "$lib/components/home/Benefits.svelte";
import CTA from "$lib/components/home/CTA.svelte";
import FAQ from "$lib/components/home/FAQ.svelte";
import Features from "$lib/components/home/Features.svelte";
import Hero from "$lib/components/home/Hero.svelte";
import SocialProof from "$lib/components/home/SocialProof.svelte";

export {Benefits, CTA, FAQ, Features, Hero, SocialProof};